server {

  listen 80;
  listen [::]:80;
  server_name {{ sonar_domain }};

  location / {
    proxy_pass        http://localhost:{{ sonar_port }};
    proxy_redirect    off;
    proxy_set_header  Host $host;
  }
}
